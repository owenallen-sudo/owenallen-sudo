# I hate sudo, obviously
Instead of the bloated sudo I use a fork of opendoas on an artix build. I also use the runit init system because it follows the unix philosophy and limine because it is less bloated than grub. I do everything but browsing from the TTY on my computer, without any multiplexer. I start sway followed by librewolf in a heavily restricted firejail if it requires browsing. It doesn't even have access to my downloads folder. I am currently also working on compiling the Linux kernel with only the drivers I need, down to my RTL8852BE wifi card, all inbuilt without modules because as you can see tux is getting fat.

# About me
I'm an open-source software developer (well more of a security, bugs and architecture type, I am horrible at remembering syntax) who works on projects such as owenallen-sudo/ish to fix bugs in ish and owenallen-sudo/multi-downloader for downloading software. I am also very interested in plan9's namespaces, seL4 and LionsOS (especially regarding it's fast IPC communication, and lock free deisgn) and beOS (and haiku) extended attributes and multithreading architecture but have never actually tried any of them.
